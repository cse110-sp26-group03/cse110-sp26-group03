/**
 * Placeholder test for the frontend test suite.
 *
 * Keeps the suite runnable until real frontend tests are added.
 */

import { test, expect } from 'bun:test';

test('placeholder', () => {
  expect(true).toBe(true);
});
