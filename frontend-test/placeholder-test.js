import { test, expect } from 'bun:test';

// Placeholder so `bun test frontend-test` has something to run.
// Replace with real frontend tests as the CLI/UI surfaces grow.
test('frontend test harness is wired up', () => {
  expect(true).toBe(true);
});
