/**
 * Placeholder unit tests for the website component loader (`website/js/include.js`).
 *
 * Replace this with real tests later
 */
import { test, expect } from 'bun:test';

/** Placeholder so `bun test website-test` has an include.js suite to run. */
test('website test harness is wired up', () => {
  expect(true).toBe(true);
});
