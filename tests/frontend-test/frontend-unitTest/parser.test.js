/**
 * Placeholder unit tests for the CLI parser (`src/cli/parser.js`).
 *
 * Replace this with real tests later
 */
import { test, expect } from 'bun:test';

/** Placeholder so `bun test frontend-test` has a parser.js suite to run. */
test('parser test harness is wired up', () => {
  expect(true).toBe(true);
});
