.DS_Store

# npm
node_modules/

# JSDoc generated HTML output
out/

# Manta local SQLite DB — derived from .manta/issues.jsonl, never commit.
# JSONL (the source of truth, per ADR-001) is NOT ignored.
.manta/*.db
.manta/*.db-journal
.manta/*.db-wal
.manta/*.db-shm

# Jekyll / Ruby (not used — static HTML site in website/)
website/vendor/
website/.bundle/
website/Gemfile.lock
